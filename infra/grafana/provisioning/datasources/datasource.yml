apiVersion: 1

datasources:
  - name: Prometheus
    # Fixed uid so the provisioned dashboards can bind to it by reference.
    uid: prometheus
    type: prometheus
    access: proxy
    url: http://prometheus:9090
    isDefault: true
    editable: false
